import { ReactNode } from "react";

export interface GenresProps {
  item: {
    name: string;
    href: string;
    icon: string;
  };
  // genres?: {
  //   name: string;
  //   href: string;
  //   icon: string;
  // };
}
